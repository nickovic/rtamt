#ifndef STL_OPERATOR_TYPE_H
#define STL_OPERATOR_TYPE_H

enum StlOperatorType {
    UNDEFINED,
    PREDICATE,
    NOT,
    OR,
    AND,
    IMPLIES,
    IFF,
    XOR,
    EV,
    ALWAYS,
    UNTIL,
    ONCE,
    HIST,
    SINCE
};

#endif /* STL_OPERATOR_TYPE_H */

